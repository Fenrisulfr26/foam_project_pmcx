"# pmcx_foam" 

This code use the MCX to fitting experiment data and generate many simulation data to train the muti-fidelity model 

echo "# foam_project_pmcx" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Fenrisulfr26/foam_project_pmcx.git
git push -u origin main
